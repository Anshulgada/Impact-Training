from abc import ABC, abstractmethod

class A1(ABC):

    @abstractmethod     # Creating AbstractMethod Class
    def A(self):
        pass            # Func name should be same for Parent and Child methods